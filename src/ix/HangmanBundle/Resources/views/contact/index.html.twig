{% extends "@Hangman/layout.html.twig" %}

{% block title 'Contact us - ' ~ parent() %}
{% block content %}
    <h1>Contact us</h1>
    <form action="{{ path('app_contact') }}" method="post" novalidate>
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Send</button>
    </form>


{% endblock %}